# .gitlab-ci.yml — Declare Availability Widget
pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html app.js style.css public/
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
